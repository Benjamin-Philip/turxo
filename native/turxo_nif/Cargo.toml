[package]
name = "turxo_nif"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37.2"
tokio = {version = "1.50.0", features = ["rt-multi-thread", "sync"]}
turso = "0.5.1"
